//Celsius para Fahrenheit = F = (C * 1.8) + 32 
//Fahrenheit para Celsius = C = (F - 32) * (5/9)
//Celsius para Kelvin = K = C + 273.15 
//Kelvin para Celsius = C = K - 273.15
//Celsius para Kelvin = K = (F - 32) * (5/9) + 273.15 
//Kelvin para Fahrenheit = F = (K - 273.15) * 1.8 + 32
import leia, { questionFloat } from 'readline-sync'

var temperatura[i] = ("Escolha a unidade de temperatura de origem:")
    var temp1 = ("Celsius")
    var temp2 = (" Kelvin")
    var temp3 = (" Fahrenheit")
    temperatura = leia.questionInt("Digite o número correspondente: ");
    
    temperatura= leia.questionFloat("Digite a temperatura de origem: ");
    
    
    var unid = ("\nEscolha a unidade de temperatura para a qual deseja converter:");
    
    temperatura = leia.questionInt("Digite o número correspondente: ");
    
    if (escolha_origem == 1) ; 
        if (temperatura == 1){
        temperatura = leia.question("Celsius para Fahrenheit = F = (C * 1.8) + 32");
        }
            
        else if (temperatura == 2);{
        temperatura = (K = (F - 32) * (5/9) )+ 273.15 
        }
        else if (temperatura  == 3);{
            temperatura = (C = (F - 32) * (5/9)) + 32  
        }
   
    console.log("temperatura conertida");
    
    
